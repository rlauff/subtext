- implement character escapes to make meta programming possible. Test with array.st once that is done

- make default implementation for Interpreter instead of writing all fields out explicitly everywhere

- in general, the code could use a small cleanup

- not include the parenthesis when parsing a function body or a scope:
    At the moment, we pass (input) as the function body to a Function object. Same with scopes. This is cumbersome as we delete them later anyway.
    Also, it causes issues where print_output will put parenthesis around its prompt. See examples/fib.st. This could also be handled manually.
    However, it seems much cleaner to not put them in in the first place

- replace linked_chars logic by using ropey. Ropey is already in the dependencies and might speed things up.
    Also, with ropey we might be able to simplify the rest of the code a bit. This also gets rid of the fact that replaced chars are not deleted in the current version.
    We could also write a garbage collector to fix this, but switching to ropey seems cleaner.

- use chars_indices() in the split functions in scope.rs. At the moment, we iter manually, which works but is needlessly complicated.

- extend std:
    extend add to negative numbers
    divide?
    modulo?
    finish array 
